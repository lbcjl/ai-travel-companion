# 阶段零：通关挑战指南 (The Challenge)

恭喜你读完了所有的理论课程。但正如我们在“心智模型”中所说：**动手写出来的才是你的**。

本指南包含两个**必须完成**的硬核任务。只有当你亲手在终端里敲完这些命令并看到预期结果后，你才算真正通过了“阶段零”。

---

## 🏆 挑战一：Git 左右互搏 (The Pull Request Simulation)

在这个挑战中，你将扮演两个角色：**开发者 (Developer)** 和 **代码审查者 (Reviewer)**。你需要模拟一次完整的团队协作流程。

### 任务目标

模拟 "Feature Branch Workflow"：创建分支 -> 修改代码 -> 解决冲突 -> 合并代码。

### 通关步骤

#### Step 1: 建立战场

创建一个新文件夹作为 Git 仓库。

```bash
mkdir git-challenge
cd git-challenge
git init
git branch -M main
echo "# Git Challenge" > README.md
git add .
git commit -m "init: project start"
```

#### Step 2: 开发者角色 - 开发新功能

你是开发者，接到了“添加用户列表”的需求。**切记：不能在主分支直接改！**

```bash
# 1. 创建并切换分支
git checkout -b feature/user-list

# 2. 模拟写代码
echo "- User 1: Alice" >> users.txt
echo "- User 2: Bob" >> users.txt

# 3. 提交代码
git add users.txt
git commit -m "feat: add basic user list"
```

#### Step 3: 模拟主分支变更 (制造冲突前兆)

现在切回 `main` 分支。你是 **Reviewer**，你发现在开发者开发的同时，主分支也更新了。

```bash
git checkout main

# 主分支也创建了 users.txt，但是内容不一样！
echo "- User 1: Admin" >> users.txt

git add users.txt
git commit -m "feat: add admin user"
```

#### Step 4: 开发者角色 - 合并代码 (遭遇冲突)

开发者想把代码合并回主分支。

```bash
# (此时我们在 main 分支)
# 尝试合并 feature 分支
git merge feature/user-list
```

🚨 **预期结果**：
你**必须**看到类似这样的报错：
`CONFLICT (add/add): Merge conflict in users.txt`
`Automatic merge failed; fix conflicts and then commit the result.`

#### Step 5: 解决冲突 (通关关键)

1. 使用 `code .` 打开 VS Code。
2. 打开 `users.txt`。
3. 你会看到 `<<<<<<< HEAD` 和 `>>>>>>> feature/user-list`。
4. **修改文件**，保留两个用户，使其变成：
   ```text
   - User 1: Admin
   - User 1: Alice
   - User 2: Bob
   ```
5. 保存文件。

#### Step 6: 提交修复

```bash
git add users.txt
git commit -m "fix: merge user list conflict"
```

✅ **如果 `git status` 显示 clean，恭喜你，挑战一通关！**

---

## 🏆 挑战二：黑客入门 (The Curl Interaction)

在这个挑战中，你将不使用浏览器，仅靠终端命令与互联网另一端的服务器进行对话。

### 任务目标

向公共测试 API (`httpbin.org`) 发送一个包含 JSON 数据的 POST 请求，并解析返回结果。

### 通关步骤

#### Step 1: 发送简单请求 (Ping)

先确认网络通畅。

```bash
curl https://httpbin.org/get
```

✅ **预期结果**：屏幕上打印出一堆 JSON，包含 `"origin": "你的IP地址"`。

#### Step 2: 伪造身份与数据 (The Attack)

我们要发送一个复杂的请求：

- **方法**: POST
- **Header**: 告诉服务器我发的是 JSON (`Content-Type: application/json`)
- **Header**: 伪造一个 Token (`Authorization: Bearer my-secret-token`)
- **Body**: 包含你的名字 (`{"name": "YOUR_NAME", "mission": "completed"}`)

请在一个命令中完成：

```bash
curl -v -X POST https://httpbin.org/post \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer my-secret-token" \
     -d '{"name": "Antigravity", "mission": "completed"}'
```

_(注：Windows CMD 用户可能需要把单引号 `'` 换成双引号 `"`，并把内部的双引号转义 `\"`，或者推荐直接使用 Git Bash 运行上述命令)_

#### Step 3: 解读战报 (Analysis)

仔细观察 `-v` (Verbose) 输出的日志：

1.  找到 `> POST /post HTTP/2` (你发出的请求行)
2.  找到 `> authorization: Bearer my-secret-token` (你伪造的 Header)
3.  找到 `< HTTP/2 200` (服务器返回的状态码)

✅ **预期结果**：
在最后的 JSON 响应中，你必须能看到服务器把你发过去的数据原样还给了你：

```json
"json": {
  "mission": "completed",
  "name": "Antigravity"
}
```

---

## 📝 总结

这两项挑战涵盖了：

1.  **版本控制之心**: 分支与解决冲突。
2.  **网络协议之魂**: HTTP 报文的构造与调试。

如果你能流畅完成以上操作，**阶段零，通关！** 🚀
下一站：**现代前端深度构建 (React & TypeScript)**。
