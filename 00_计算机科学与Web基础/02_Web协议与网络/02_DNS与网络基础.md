# 02. DNS 与网络基础 - 互联网通讯录

假如你要给你的朋友“张三”打电话，你会直接背下来他的手机号 `138-1234-5678` 吗？
通常不会。你会打开通讯录，搜索“张三”，然后点击拨号。

**DNS (Domain Name System)** 就是互联网的通讯录。

## 🛠️ 准备工作：运行环境

本节涉及 `curl` 和 `nslookup` 命令。

1.  **Windows 用户**: 推荐使用 **Git Bash**。
    - 如果使用默认 CMD，`nslookup` 可以正常工作。
    - 但 `curl` 可能遇到引号转义问题，且没有 `dig` 命令。
2.  **Mac/Linux 用户**: 直接使用终端即可。

---

---

## 🎭 场景一：寻找“谷歌之家” (DNS Resolution)

在浏览器输入 `www.google.com` 并回车时，发生了一场跨越全球的接力赛。

### 1. 本地通讯录 (Browser & System Cache)

- **浏览器**：“等等，我 5 分钟前刚去过，我记得地址是 `142.250.190.46`。”（直接命中，速度 0ms）。
- **系统 (`/etc/hosts`)**：“我也看看我的小本本...没记。”

### 2. 小区物业 (ISP DNS)

如果电脑不知道，它会问你家宽带运营商（电信/联通）的 DNS 服务器。

- **ISP DNS**：“我查查缓存...也没有。但我知道谁管这事。”

### 3. 全球接力 (Recursive Query)

- **根服务器 (.)**：“我不记具体地址，但我知道 `.com` 归谁管，去找 `.com` 顶级域服务器。”
- **顶级域 (.com)**：“我不记 `google.com`，但我知道它归 `ns1.google.com` 管，你去问它。”
- **权威服务器 (ns1.google.com)**：“找到了！`www.google.com` 的 IP 是 `142.250.190.46`。”

### 4. 拿到结果

浏览器拿到了 IP，终于可以建立 TCP 连接，发送 HTTP 请求了。

---

## 🎭 场景二：IP 地址的秘密 (localhost vs 127.0.0.1)

经常有新手搞混这三个概念：

1.  **`127.0.0.1` (环回地址)**

    - **比喻**: 这是一个**死胡同**。当你发数据给这个 IP，网卡根本不会把数据发到网线上去，而是原地掉头转给自己。
    - **作用**: 测试本机的服务（比如你启动的 `localhost:8080`）。

2.  **`0.0.0.0` (任意地址)**

    - **比喻**: 这是一个**广播站**。
    - **场景**: 当你的 Go/Node 服务器监听 `0.0.0.0:8080` 时，意味着：“不管你是从本机访问，还是从局域网（同事电脑）访问，还是从公网访问，我全接单。”
    - **坑点**: 如果你只监听 `127.0.0.1:8080`，那你同事就访问不了你的服务了。

3.  **`localhost` (域名)**
    - **比喻**: 这只是一个**名字**。
    - 通常，系统的 `/etc/hosts` 文件里写着：`127.0.0.1 localhost`。所以它等价于 `127.0.0.1`。但如果你恶作剧把 host 改了，`localhost` 也可以指向百度。

---

## 🛠️ 实战调试技巧

### 1. 看看我的 IP 是什么

- **内网 IP**: `ipconfig` (Windows) 或 `ifconfig` (Mac/Linux)。
- **公网 IP**: `curl ifconfig.me`。这是全世界都能看到的那个 IP。

### 2. DNS 挖坟 (`nslookup` / `dig`)

想知道 `baidu.com` 背后到底有多少个 IP？

```bash
nslookup baidu.com

# 输出:
# Name:    baidu.com
# Addresses: 220.181.38.148, 220.181.38.251
```

这说明百度用了**负载均衡**，同一个域名对应多个 IP，谁有空谁接客。

---

> 💡 **核心最佳实践**
>
> 1.  **TTL (Time To Live)**: DNS 是有缓存时间的。如果你换了服务器 IP，用户可能要等 TTL 过期（比如 10 分钟甚至 24 小时）才能访问到新 IP。**切库前记得把 TTL 调短**。
> 2.  **Hosts 文件**: 开发时想在本地测试 `api.my-pro-site.com`？不用买域名，直接修改本机的 `/etc/hosts` (Windows 在 `C:\Windows\System32\drivers\etc\hosts`)，加上一行 `127.0.0.1 api.my-pro-site.com` 即可。这是骗过浏览器的最快方法。
